server:
  sources:
    - path: /data
      name: data
      config:
        defaultEnabled: true
auth:
  methods:
    password:
      enabled: false
    oidc:
      enabled: true
      clientId: "filebrowser"
      clientSecret: "insecure_secret"
      issuerUrl: "http://authelia.authelia.svc.cluster.local:9091"
      scopes: "email openid profile groups"
      userIdentifier: "preferred_username"
      disableVerifyTLS: false
      logoutRedirectUrl: ""
      createUser: true
      adminGroup: "admin"
      groupsClaim: "groups"
